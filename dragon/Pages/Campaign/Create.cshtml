@page
@model dragon.Pages.Campaign.CreateModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Ad</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post" enctype="multipart/form-data">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Ad.Title" class="control-label"></label>
                <input asp-for="Ad.Title" class="form-control" />
                <span asp-validation-for="Ad.Title" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Ad.AdImage" class="control-label"></label>
                <input type="file" id="file1"
                       name="file1"
                       multiple="multiple" />
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
